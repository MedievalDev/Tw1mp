__pycache__/
*.pyc
ServerData.db
PlayerData/
Config.ini
# Local registry backups from setup (contain the machine's server list)
*.reg
# Runtime logs
*.log
directplay_fix_log.txt
# Native build artifacts (build the shim from source via build.bat)
*.obj
*.exp
*.lib
*.dll
# LGPL DirectPlay8 headers - fetched, not committed (see fetch-headers.sh)
directplay-shim/include/
directplay-replace/include/
